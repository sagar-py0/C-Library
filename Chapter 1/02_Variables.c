#include<stdio.h>
 int main(){
    int a;
    a = 1;
    printf("%d",a);
   return 0;
}


// 1.No commas, blanks are allowed.
// 2.Only underscore is allowed (_) from special characters.
// 3.First letter must be lower case alpabet or underscore(_).
